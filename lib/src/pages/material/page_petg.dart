import 'package:flutter/material.dart';

class PagePETG extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Material: PETG"),
      ),
    );
  }
}